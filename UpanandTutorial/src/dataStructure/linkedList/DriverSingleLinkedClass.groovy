package dataStructure.linkedList

class DriverSingleLinkedClass {
    public static void main(String[] args) {
        SingleLinkedList singleLinkedList = new SingleLinkedList()
        singleLinkedList.createSingleLinkedList(5)
        singleLinkedList.insertInLinkedList(12,1)
        singleLinkedList.display()
        //println("before deletting ")
        //singleLinkedList.traverseLinkedList()
        //singleLinkedList.deleteLinkedList()
        //println("After deleting")
       // singleLinkedList.deleteNode(8)
       /* boolean var = singleLinkedList.search(9)
        if(!var){
            println("Element does not found")
        }
        singleLinkedList.display()
       // singleLinkedList.deletionOfNode(9)
        //singleLinkedList.traverseLinkedList()
        println("Linked list is:")
        singleLinkedList.traverseLinkedList()
        println("Reverse linked list is:")
        singleLinkedList.reverse()
        singleLinkedList.traverseLinkedList()*/
    }
}
