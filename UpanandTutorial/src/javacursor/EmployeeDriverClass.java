package javacursor;

import java.util.ArrayList;
import java.util.Collections;
import java.util.Comparator;

public class EmployeeDriverClass {
    /*public static void main(String[] args) {
        ArrayList<Employee> employees = new ArrayList<Employee>();
        employees.add(new Employee(12,"upanand",123456));
        employees.add(new Employee(1,"virat",123451));
        employees.add(new Employee(2,"kohli",123422));

     //  Collections.sort(employees,(E1,E2)->(E1.name.compareTo(E2.name)));

        Collections.sort(employees, Comparator.comparing(e -> e.name));

        Collections.sort(employees, Comparator.comparingInt(e -> -e.empId));

        for(Employee employee: employees){
            System.out.println(employee.name+" "+employee.empId);
        }*/

}
